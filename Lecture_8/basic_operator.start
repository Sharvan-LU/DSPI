var a is number
var b is number
var name is text
var lastname is text

a -> 8
b -> 2
name -> "Test "
lastname -> "Student"

print("The sum of a and b is", +(a,b), ", the difference is", -(a,b),", a and b (logical AND) is", &(a,b), ", a smaller than b is", <(a,b), ", the full name is ", ++(name, lastname))
